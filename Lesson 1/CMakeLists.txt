#TODO 1: we always need to specify minimum cmake requirement
cmake_minimum_required(VERSION 3.1)

#TODO 2: we always need to assign the project name
project(SimpleCmakeProject)

#TODO 3: make our main.cpp executable
add_executable(myExecutable main.cpp)
